# Test Dependencies for Code-Review Project# Test requirements for the data pipeline unit tests

# Core testing frameworkpytest>=7.0.0

pytest>=7.4.3pytest-mock>=3.10.0

pytest-cov>=7.0.0pytest-cov>=4.0.0

pytest-mock>=3.15.1unittest-xml-reporting>=3.2.0

pytest-html>=4.0.2mock>=4.0.3

pytest-metadata>=3.0.0

pytest-xdist>=3.8.0# PySpark for hybrid testing (real execution)

pyspark>=3.3.0,<4.0.0

# PySpark and related dependencies

pyspark>=3.5.0# Optional: For more advanced testing

pyarrow>=10.0.0pytest-xdist>=3.0.0  # For parallel test execution

pandas>=2.0.0pytest-html>=3.1.0   # For HTML test reports

pytest-bdd>=6.0.0    # For behavior-driven development testing
# Mocking and test utilities
mock>=5.0.0
unittest-mock>=1.0.0

# Coverage reporting
coverage>=7.0.0

# Code quality and linting (optional but recommended)
flake8>=6.0.0
black>=23.0.0
isort>=5.12.0

# Additional utilities
setuptools>=65.0.0
wheel>=0.40.0
pip>=23.0.0

# For Windows compatibility
pywin32>=306; sys_platform == "win32"
wmi>=1.5.1; sys_platform == "win32"

# XML parsing for test results
lxml>=4.9.0

# Environment and configuration
python-dotenv>=1.0.0